# Examples

Examples should demonstrate KNI compilation, signed bundle loading, local KLIQ state and Reality Graph queries without embedding secrets.

- `systemd/kliq.service` shows the managed KLIQ daemon shape without embedding secrets.
